# Fuse — 웹 퍼즐 게임 사이트

## 프로젝트 개요
- **사이트**: https://fusepuzzle.com — 심심할 때 즐기는 무료 웹 퍼즐 게임 모음.
- **목표**: 게임을 하나씩 늘려 방문자의 체류시간·페이지뷰를 키우고, Google AdSense로 수익화한다.
- **운영자**: 1인 개발자. Windows 앱(C#/C++) 개발자 출신이라 코드 리뷰·수정은 익숙하다. 답변·설명은 한국어(존댓말)로.

## 현재 상태 (2026-08)
- 게임 6개 라이브: `/fuse/`(2048 변형), `/sudoku/`, `/sequence/`(순서 기억), `/reaction/`, `/color-spot/`, `/math/`.
- **허브 구조로 전환 완료.** 루트 `index.html`은 게임 목록 허브이고, 기존 2048 게임은 `/fuse/`로 이동했다.
- 공략·읽을거리 `/guide/` 아래에 글 7편. 전체 18페이지 규모.
- **GitHub Pages**로 배포(main 브랜치 → 자동 반영). 커스텀 도메인은 가비아 DNS로 연결, HTTPS 강제 적용됨.
- **Search Console** 소유권 확인 완료, `sitemap.xml` 제출됨.

### AdSense 상태 — 중요
- **1차 심사에서 "가치 없는 콘텐츠(Low value content)" 사유로 거절되었다.**
- 원인: 당시 라이브 사이트가 **4페이지·본문 약 3,200자·게임 1개**뿐이었다. 게임 5종을 "승인 후 반영"으로 미뤄둔 것이 오히려 거절을 불렀다.
- 대응: 허브 전환 + 게임 6개 + 공략 글 7편으로 개편해 **18페이지·약 3만 자** 규모로 키웠다.
- 재심사 전 절차: sitemap 재제출 → 주요 페이지 색인 요청 → **1~2주 대기(색인 반영)** → 재심사 신청.
- **교훈: 심사를 기다리며 콘텐츠를 묶어두지 않는다.** 만든 것은 바로 배포해 사이트를 키우는 쪽이 승인에 유리하다.

## 기술 스택 / 제약 (중요)
- **빌드 과정 없음.** 순수 정적 사이트. 번들러·프레임워크·npm 의존성 없이 브라우저에서 바로 열리는 HTML/CSS/JS만 쓴다.
- 게임은 **단일 HTML 파일**로 자기완결적으로 작성한다(해당 게임 폴더의 `index.html`에 CSS·JS 인라인). 콘텐츠 페이지(허브·공략·안내)는 `site.css`를 링크해 공용 스타일을 쓴다.
- **바닐라 JS만.** React 등 프레임워크 금지.
- **localStorage는 반드시 try-catch로 감싸고 메모리 폴백**을 둔다(샌드박스·프라이빗 모드에서 예외로 죽지 않게).
- **모바일 우선.** 트래픽 대부분이 모바일이다. 터치/스와이프 입력, 반응형 레이아웃, `viewport-fit=cover` 필수.
- 접근성 하한선: 키보드 조작 지원, `prefers-reduced-motion` 존중, 포커스 가시성 유지.

## 디자인 토큰 (site.css와 일치시킬 것)
- 배경 `#10141f` (다크), 패널 `#1b2130`, 잉크 `#e8ecf4`, 뮤트 `#8b93a7`.
- 강조 그라데이션: `#5b8cff` → `#8b5cf6` (로고·버튼·주요 액션). 로고 워드마크는 이 그라데이션 텍스트.
- 타일 색상 스케일(값이 커질수록 차가운 색→뜨거운 색): teal→green→lime→gold→amber→orange→coral→pink→magenta→violet.
- 폰트: **Outfit** (Google Fonts), 굵기 400/500/600/700/800. 시스템 폰트 폴백 포함.
- 새 게임도 이 톤(다크+그라데이션)을 따라 사이트 전체가 하나의 브랜드로 보이게 한다.

## 사이트 구조
```
/                    → 게임 목록 허브
/fuse/               → 숫자 합치기 (2048 변형)
/sudoku/             → 스도쿠 (난이도 3단계 + 도전 모드)
/sequence/           → 컬러 시퀀스 (순서 기억)
/reaction/           → 반응속도 테스트
/color-spot/         → 색 찾기
/math/               → 빠른 암산
/<game>/             → 이후 추가되는 게임들
/guide/              → 공략·읽을거리 인덱스
/guide/<slug>/       → 개별 공략 글
about.html, privacy.html, how-to-play.html → 공통 안내
```
- 각 게임 페이지 하단에 **다른 게임으로 가는 링크**를 둬 게임 간 이동을 유도한다(체류시간·수익 상승).
- 게임이나 글을 추가하면 **`sitemap.xml`·허브 목록·`/guide/` 인덱스**를 함께 갱신한다.

## 각 게임 페이지 필수 요소
새 게임을 만들 때 아래를 빠짐없이 포함한다.
- `<head>`에 공통 태그: favicon 링크, AdSense 스크립트, Open Graph/Twitter 태그, canonical, google-site-verification. (기존 페이지의 head를 참고해 동일 패턴 유지.)
- 광고 자리: 비어 있을 때 안 보이는 `<div class="ad-slot">` (`.ad-slot:empty{display:none}` 규칙 사용). 승인 후 광고 코드 삽입.
- 상단 브랜드·하단 푸터를 다른 페이지와 통일하고, 푸터에 홈·공략 링크를 넣는다.
- **본문 콘텐츠 최소 800자** — 게임 방법, 요령, 기록 안내를 페이지 안에 글로 담는다. (AdSense 심사에서 텍스트 분량이 실제로 문제가 됐으므로 짧게 넘어가지 않는다.)
- 관련 공략 글이 있으면 본문에서 링크한다.
- 최고 기록 저장, 결과 화면에 등급/상위 % 표시(다른 게임과 통일).

## 콘텐츠 원칙
- 공략 글은 **실제로 도움이 되는 내용**으로 2,000자 이상 쓴다. 게임 유입뿐 아니라 검색 유입도 노린다.
- 인지 능력·건강 관련 서술은 **과장하지 않는다.** 연구 견해가 갈리는 부분은 갈린다고 쓰고, 글 하단에 의학적 자료가 아니라는 안내를 넣는다.
- 등급·상위 %는 재미용 추정치임을 화면에 명시한다. **IQ 같은 표현은 쓰지 않는다**(부정확하고 심사에도 불리하다).

## 저작권·상표 주의
- **게임 규칙·메커니즘은 저작권 대상이 아니므로 자유롭게 구현 가능**하지만, **이름·로고·고유 아트는 보호된다.**
- 실제로 첫 순서 기억 게임을 "Simon"으로 만들었다가 Hasbro 등록상표라 **"컬러 시퀀스"/`/sequence/`로 변경**했다.
- 새 게임은 **직접 지은 서술형 한국어 이름**을 쓴다(반응속도, 색 찾기, 빠른 암산처럼). Tetris·Monopoly·Catan 등 브랜드 게임의 이름·아트·특유의 룩은 쓰지 않는다.

## 로드맵 / 다음 작업
- **재심사 준비**: sitemap 재제출 → 색인 요청 → 1~2주 대기 → AdSense 재심사 신청.
- 콘텐츠는 계속 늘린다. 공략 글 추가가 게임 추가만큼 중요하다.
- 이후 게임 후보: 15 퍼즐(슬라이드), 패턴 기억(공간 기억), 스트룹(색·글자 반사), 짝 맞추기 등. 기존 게임과 인지 유형이 겹치지 않는 것 위주로.

## 작업 방식
- 변경은 **브랜치 + PR**로 제안하고, 운영자가 검토 후 머지하면 main에 반영(=배포)된다.
- 커밋/PR 설명은 무엇을 왜 바꿨는지 한국어로 간단히.
- 게임을 만들면 **브라우저로 실제 동작을 검증**한다(렌더링·조작·기록 저장·콘솔 오류). 페이지를 추가하면 링크·canonical이 깨지지 않았는지 확인한다.

## 저장소에 있는 자산 파일
`favicon.svg`, `favicon-32.png`, `favicon-16.png`, `apple-touch-icon.png`, `og-image.png`, `robots.txt`, `sitemap.xml`, `ads.txt` — 모두 저장소 root에 위치. 새 게임을 만들 때 재사용/갱신.
